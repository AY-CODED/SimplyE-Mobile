const Logo = function ({ size }) {
    return (
        <div className="flex items-center justify-center h-20">
            <img src="/Logo.png" alt="Logo" className="w-full h-full object-center object-cover" />
        </div>
    );
};

export default Logo;
